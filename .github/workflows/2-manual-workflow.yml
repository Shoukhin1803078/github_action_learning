name: Manual Workflow

on:
  workflow_dispatch:

jobs:
  manual-workflow:
    runs-on: ubuntu-latest

    steps:
      - name: First Manual Workflow Step
        run: |
          echo "Hello, World!"
          echo "Hello Again!"
