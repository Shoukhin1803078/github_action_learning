name: Manual Workflow

on:
  workflow_dispatch:            # This is the event that will trigger the workflow manually

jobs:
  manual-workflow:               # This is the name of the job
    runs-on: ubuntu-latest

    steps:
      - name: First Manual Workflow Step
        run: |
          echo "Hello, World!"
          echo "Hello Again!"
